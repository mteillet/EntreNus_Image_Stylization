# Python_3D_IMG_Processing
 Images Processing in Python aimed towards stylization of 3D rendering passes

# Install
the following packages have to be installed :

# Python 3.7+

# OpenCV
    "pip install opencv-python"
# Matplotlib
    "pip install matplotlib"
# pyQt5
    "pip install pyqt5"
